import { makeStyles } from "@material-ui/core";

export const S_customIconButton = makeStyles(theme => ({
  rootCustomIconButton: {
    color: theme.palette.primary.main,
    padding: "0px",
  },
}));
